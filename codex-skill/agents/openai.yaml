interface:
  display_name: "CLI-Anything"
  short_description: "Build, refine, test, validate, list, or install CLI-Anything harnesses from Codex."
  default_prompt: "Use CLI-Anything to build, refine, test, validate, list, or install a harness for the user's target software or repository."
